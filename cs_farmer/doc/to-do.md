1. Change that the seeds and harvest inventory box is below the field 

2. Add a new function called reset_field() 
- it erases the full board (all plants are gone) and sets the pointer back to 0, 0 

3. Add the if() functionality 
- in the same python style as while 
if(...):
    code
- it is classic boolean dependant, we are implementing more bool returning functions later

8. update the documentation
- give me a feedback in the chat no need to also write something in this file

