1. add a break() statement, so that a while loop can be interrupted

2. add string comparisons for loops and if statements
- for example at the moment you cannot compare if(get_direction() == "right"):

3. add for loop to the game
- it should work like in python with a range
- example: for i in range (5):

4. check get_inventory(wheat)
- cannot use get_inventory() in sell function

3. update the documentation (some of the last changes might be missing)
- give me a feedback in the chat no need to also write something in this file

