
## Code and progress

- Let players create their own variables.
- Make the `for` loop name available inside its body, such as using `i` in `print(i)`.
- Add Python-style `range(start, stop)` and `range(start, stop, step)` forms. The current language supports only `range(count)`.
- Let players define their own functions.

These items are ideas only and are not implemented yet.
